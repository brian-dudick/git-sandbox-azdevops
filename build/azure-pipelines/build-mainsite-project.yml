parameters:
  runExtendedTests: 'false'  # defaults for any parameters that aren't specified

steps:
- ${{ if eq(parameters.runExtendedTests, 'false') }}:
  - script: echo runExtendedTests $(Build.SourceBranchName) 
displayName: 'Run MainSite'