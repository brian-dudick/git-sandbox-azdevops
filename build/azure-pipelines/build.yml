parameters:
  toolset: msbuild

steps:
# msbuild
- ${{ if contains(variables['Build.SourceBranch'], 'Features') }}:
  - task: msbuild@1
  - task: vstest@2

# dotnet
#- ${{ if !contains(variables['Build.SourceBranch'], 'Features') }}:
#  - task: dotnet@1
#    inputs:
#      #command: build
#  - task: dotnet@1
#    inputs:
#      #command: test