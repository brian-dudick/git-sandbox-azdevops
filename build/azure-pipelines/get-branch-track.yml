steps:
- powershell: |
   $branchSource = "$(Build.SourceBranchName)"

   write-host "The Branch Source is: $branchSource" 

   Write-Host "Using Department: $($env:BranchTrack)"

   Get-ChildItem Env:
  displayName: 'Get The Branch Track'